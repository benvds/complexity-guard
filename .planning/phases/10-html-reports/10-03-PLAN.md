---
phase: 10-html-reports
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - README.md
  - docs/getting-started.md
  - docs/cli-reference.md
  - docs/examples.md
  - publication/npm/README.md
  - publication/npm/packages/darwin-arm64/README.md
  - publication/npm/packages/darwin-x64/README.md
  - publication/npm/packages/linux-arm64/README.md
  - publication/npm/packages/linux-x64/README.md
  - publication/npm/packages/windows-x64/README.md
autonomous: true
requirements:
  - OUT-HTML-01
  - OUT-HTML-02
  - OUT-HTML-03
  - OUT-HTML-04

must_haves:
  truths:
    - "README.md mentions HTML report format as a feature"
    - "docs/cli-reference.md documents --format html option"
    - "docs/getting-started.md mentions HTML reports in the output formats overview"
    - "docs/examples.md has an HTML Reports section with usage examples"
    - "Publication READMEs mention HTML report capability"
  artifacts:
    - path: "README.md"
      provides: "HTML report feature mention"
      contains: "html"
    - path: "docs/cli-reference.md"
      provides: "HTML format documentation"
      contains: "--format html"
    - path: "docs/examples.md"
      provides: "HTML report examples"
      contains: "HTML"
  key_links:
    - from: "README.md"
      to: "docs/examples.md"
      via: "documentation link"
      pattern: "examples"
    - from: "docs/getting-started.md"
      to: "docs/cli-reference.md"
      via: "format reference link"
      pattern: "cli-reference"
---

<objective>
Update all documentation to reflect the new HTML report output format.

Purpose: Ensures users can discover and learn how to use `--format html` through README, getting-started guide, CLI reference, and examples. Keeps publication READMEs in sync per CLAUDE.md requirements.

Output: Updated README.md, docs pages, and publication READMEs documenting HTML report capabilities.
</objective>

<execution_context>
@/Users/benvds/.claude/get-shit-done/workflows/execute-plan.md
@/Users/benvds/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-html-reports/10-CONTEXT.md
@.planning/phases/10-html-reports/10-01-SUMMARY.md
@.planning/phases/10-html-reports/10-02-SUMMARY.md
@README.md
@docs/getting-started.md
@docs/cli-reference.md
@docs/examples.md
@publication/npm/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md and docs pages with HTML report documentation</name>
  <files>
    README.md
    docs/getting-started.md
    docs/cli-reference.md
    docs/examples.md
  </files>
  <action>
**README.md:**
- Add "HTML Reports" to the output formats feature list (alongside Console, JSON, SARIF)
- Update the `--format` description in the CLI flags section to include `html`
- Add a brief mention in the features section: self-contained HTML reports with interactive dashboard, treemap visualization, sortable metric tables
- If there's a format/output example section, add a one-liner: `complexity-guard --format html --output-file report.html src/`

**docs/getting-started.md:**
- Add HTML to the output formats overview section (where console, JSON, SARIF are listed)
- Brief description: "HTML reports generate a self-contained file with an interactive dashboard, file breakdown, and visualizations. Ideal for sharing with stakeholders or reviewing in a browser."
- Example command: `complexity-guard --format html --output-file report.html src/`
- Note that `--output-file` is recommended for HTML (output is large)

**docs/cli-reference.md:**
- Update `--format` option to list `html` as a valid value: `console | json | sarif | html`
- Add HTML-specific documentation: describe what the report contains (dashboard, file table, treemap, bar chart)
- Document that HTML output is self-contained (no external CSS/JS dependencies)
- Note `--output-file` usage for saving to disk

**docs/examples.md:**
- Add an "HTML Reports" section following the same pattern as SARIF Output section
- Include examples:
  - Basic: `complexity-guard --format html --output-file report.html src/`
  - With custom thresholds: `complexity-guard --format html --output-file report.html --error 25 src/`
  - Open in browser after generation: mention `open report.html` (macOS) or `xdg-open report.html` (Linux)
- Describe what the report contains: project health dashboard, interactive file drill-down, treemap visualization, sortable tables
  </action>
  <verify>Read each updated file and confirm HTML report documentation is present. Check that all format lists include `html`. Verify no broken markdown links.</verify>
  <done>README.md lists HTML as an output format. docs/getting-started.md describes HTML reports. docs/cli-reference.md documents --format html. docs/examples.md has HTML Reports section with usage examples.</done>
</task>

<task type="auto">
  <name>Task 2: Update publication READMEs to include HTML report capability</name>
  <files>
    publication/npm/README.md
    publication/npm/packages/darwin-arm64/README.md
    publication/npm/packages/darwin-x64/README.md
    publication/npm/packages/linux-arm64/README.md
    publication/npm/packages/linux-x64/README.md
    publication/npm/packages/windows-x64/README.md
  </files>
  <action>
**publication/npm/README.md:**
- Sync with main README.md changes — add HTML report format to output formats list
- Add `html` to any format enumeration
- Add brief HTML report mention if there's a features list

**Platform package READMEs (all 5):**
- Each has a "What ComplexityGuard Measures" section — add "HTML Reports" bullet to output capabilities if there's an output formats subsection
- If there's a format list, add `html` alongside console, json, sarif
- Keep changes minimal and consistent across all 5 packages

Follow the same pattern used in Phase 09 where SARIF was added to these READMEs: typically a single bullet point addition to the output/features section.
  </action>
  <verify>Read each publication README and confirm HTML is mentioned in the output formats. Verify all 6 publication READMEs are consistent with each other and with main README.</verify>
  <done>All publication READMEs mention HTML report format. Content is consistent across main README and all 6 publication READMEs.</done>
</task>

</tasks>

<verification>
1. README.md mentions --format html and HTML report feature
2. docs/getting-started.md covers HTML reports in output formats
3. docs/cli-reference.md documents --format html with description
4. docs/examples.md has HTML Reports section with commands
5. publication/npm/README.md synced with main README
6. All 5 platform READMEs mention HTML reports
7. No broken markdown links in any updated file
</verification>

<success_criteria>
- All user-facing documentation reflects the new HTML report format
- README.md, getting-started, cli-reference, examples all updated consistently
- All 6 publication READMEs in sync with main README
- Documentation follows existing style (TanStack/Astro friendly, progressive disclosure)
- No markdown formatting issues
</success_criteria>

<output>
After completion, create `.planning/phases/10-html-reports/10-03-SUMMARY.md`
</output>
