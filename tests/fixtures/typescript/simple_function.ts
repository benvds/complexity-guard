// Fixture: Simple function
// Purpose: Baseline for minimal complexity - pure transformation with no branching
// Expected complexity: cyclomatic ~1, cognitive ~0, nesting ~0

export function greet(name: string): string {
  return `Hello, ${name}!`;
}
